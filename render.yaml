services:
  - type: web
    name: my-proactive-app
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run home.py --server.port=$PORT --server.address=0.0.0.0"